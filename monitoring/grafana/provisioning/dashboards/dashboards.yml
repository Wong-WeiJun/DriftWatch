apiVersion: 1

providers:
  - name: DriftWatch
    orgId: 1
    folder: DriftWatch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
